# TypeScript Coding Style

- Use TypeScript for application and shared package code.
- Prefer small, focused modules with clear responsibilities.
- Use camelCase for files and functions.
- Use PascalCase for React components and classes.
- Keep domain logic outside UI components when it can be reused or tested independently.

