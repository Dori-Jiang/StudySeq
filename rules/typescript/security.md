# TypeScript Security

- Treat local files and imported resources as user-controlled input.
- Validate file paths before opening local resources.
- Do not store secrets in source files.
- Keep cloud sync and account features out of the first offline-first version.

