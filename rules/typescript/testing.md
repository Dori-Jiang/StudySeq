# TypeScript Testing

- Use `*.test.ts` and `*.test.tsx` for test files.
- Prefer focused unit tests for domain logic.
- Add integration or end-to-end tests for important user workflows once the app shell exists.

